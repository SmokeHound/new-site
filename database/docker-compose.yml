version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: site_db_87yi
      POSTGRES_USER: db_owner
      POSTGRES_PASSWORD: 6uAtH6rgMVg7XIzBbbfTSBzUOLcDGwaN
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./schema.sql:/docker-entrypoint-initdb.d/schema.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d admin_dashboard"]
      interval: 30s
      timeout: 10s
      retries: 5

volumes:
  postgres_data: